# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environments
.venv/
venv/
env/

# Environment files
.env
.env.*
# ... mas o modelo (sem password) fica versionado:
!deploy/.env.beta.exemplo

# Contas MySQL geradas pelo scripts/gerar_contas_mysql.py.
# LEVAM AS PASSWORDS DE TODA A GENTE em texto simples: nunca podem ir para o
# repositorio. Usam-se uma vez (correr o .sql, distribuir o .txt) e apagam-se.
contas_martelo.sql
contas_martelo.txt
contas_martelo*.sql
contas_martelo*.txt

# Build do executavel / instalador
/build/
/dist/
installer/Output/
*.log

# Backups da BD (dumps locais antes de migrar; NUNCA versionar).
# Levam dados de clientes, hashes das contas e as passwords das ligacoes
# externas em claro -- e este repositorio e' PUBLICO.
deploy/backups/
backup_*.sql
*_backup.sql
*.sql.gz
dump_*.sql
!.env.example

# Test and coverage output
.pytest_cache/
.coverage
htmlcov/

# Build output
build/
dist/
*.egg-info/

# Tooling
.mypy_cache/
.ruff_cache/

# IDE and OS files
.idea/
.vscode/
.DS_Store
Thumbs.db

# Source data spreadsheets (analysed, not versioned)
*.xlsm
*.xlsx
*.xls

# Exported reports (output do "Exportar PDF" do Ponto Situacao)
/Ponto_Situacao.pdf

# Permissoes pessoais do Claude Code (nao partilhar); os guardas
# .claude/settings.json e .claude/hooks/ SAO para commitar.
.claude/settings.local.json
.claude/worktrees/

